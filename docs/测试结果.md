# 测试结果

测试日期：2026-09-19

## 自动化测试

执行命令：

```bash
npm run typecheck
npm test
npm run build
```

结果：

- TypeScript 与 Vue 类型检查通过。
- Vitest 共 2 个测试文件、24 个测试全部通过。
- 其中 REST API 测试 16 项，领域逻辑测试 8 项。
- Vite 生产构建成功，产物位于 `dist/`。
- 构建仅提示主 JavaScript 包较大，不影响本地比赛演示；后续公网版本可按路由拆包。

## 覆盖范围

- 预测参数对趋势曲线的影响与可复现性。
- 近期线性趋势基线和情景范围计算。
- 离线与数据不足时的预测限制。
- 任务的待执行、执行中、成功和失败状态流转。
- 离线设备限制、任务防重复和重启中断处理。
- 演示状态重置及独立测试数据库清理。
- DeepSeek 未配置、超时和失败时的接口响应；自动测试不请求真实模型。
- 设备、预测、对话、任务和仿真控制 REST 接口。

## 浏览器验收

| 场景 | 结果 |
| --- | --- |
| 390 × 844 手机视口 | 无水平溢出，三页导航与主要操作正常 |
| 360 × 800 窄屏视口 | 无水平溢出，中文界面与底部导航正常 |
| 中英文切换 | 通过 |
| 仿真问答与依据展示 | 通过 |
| 调参后重新预测 | 通过 |
| 在线设备多选 | 通过 |
| 创建 60 ml 模拟浇水任务 | 成功完成并生成回执 |
| 故障注入与一键重置 | 通过 |
| 浏览器控制台 | 最终复核无 warning 或 error |

## 当前边界

- 当前未配置 DeepSeek 密钥，因此已验收仿真问答与真实 AI 的失败提示，尚未产生真实模型调用结果。
- 所有设备、湿度、预测、供水和回执均明确标为仿真。
- 本轮未进行阿里云部署；部署步骤、宝塔配置和数据库备份恢复命令已经交付。
